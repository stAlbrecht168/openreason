# Future MCP Architecture

A future OpenReason MCP server could expose tools such as:

- `validate_frameworks`
- `analyze_input`
- `create_framework`
- `run_tests`
- `verify_framework`
- `search_frameworks`

This would allow Claude Desktop, Claude Code, Open WebUI, or other MCP-compatible clients to operate OpenReason directly.

Current status: design only.
