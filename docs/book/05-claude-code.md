# Claude Code Workflow

Claude Code is the easiest current interface for OpenReason.

The user writes natural instructions. Claude Code reads the repository, runs checks, and produces analyses using the framework files.

This lets non-technical users interact with the project without memorizing command-line steps.
