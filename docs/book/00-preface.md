# Preface

OpenReason began with a simple question:

How can an AI analyze political rhetoric without hiding the method it uses?

The answer is not to trust the AI more. The answer is to make the analysis more inspectable.

OpenReason treats frameworks as explicit artifacts. Instead of embedding theories invisibly inside prompts, it stores them as readable YAML files with triggers, concepts, rules, limitations, and references.
